in: query
name: card_reference
description: |
  Retrieves all statements related to a card. Value has to be a functional identifier for the card that can be 
  retrieved with the GET /cards endpoint.
required: false
schema:
  $ref: ../../schemas/CardReference.yaml