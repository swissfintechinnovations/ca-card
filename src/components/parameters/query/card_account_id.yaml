in: query
name: card_account_id
description: |
  Retrieves all statements related to a card account. Value has to be a technical identifier for the card account that can be 
  retrieved with the GET /card-accounts endpoint.
required: false
schema:
  $ref: ../../schemas/CardAccountId.yaml