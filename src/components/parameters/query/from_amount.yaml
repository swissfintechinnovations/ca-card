in: query
name: from_amount
description: |
  Retrieves all instances of the entity with an amount greater than or equal to the specified amount. 
required: false
schema:
  $ref: ../../schemas/generic/Amount.yaml