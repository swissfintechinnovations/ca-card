in: query
name: to_amount
description: |
  Filters resources to return only those with open an amount less than or equal to the specified amount.
required: false
schema:
  $ref: ../../schemas/generic/Amount.yaml