in: query
name: transaction_status
description: Filters resources to return only those related to the specified transaction status. Only one status can be filtered
  at a time.
required: false
schema:
  $ref: ../../schemas/TransactionStatus.yaml
