headers:
  Content-Language:
    $ref: ../headers/Content-Language.yaml
  X-Correlation-ID:
    $ref: ../headers/X-Correlation-ID.yaml
description: Internal Server Error - The request could not be processed due to a problem at the data provider. This could be for any of the reasons of the HTML standard that would result in a 5xx code.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/CommonErrorResponse.yaml
