in: query
name: from_date
required: false
schema:
  $ref: '../../schemas/shared/DateTime.yaml'
description: >
  Retrieves all transactions that took place after the provided date time. Filtering is applied on the transaction_date_time
  attribute.