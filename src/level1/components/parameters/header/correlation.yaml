in: header
name: X-Correlation-ID
required: false
schema:
  $ref: ../../schemas/shared/Correlation.yaml
description: >-
  Unique ID (defined by the caller) which will be reflected back in the
  response.
